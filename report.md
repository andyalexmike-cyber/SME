# RTT 기반 실내 측위: 제약 최적화 접근

**학번**: 12223645  
**이름**: 이윤재

---

## 1. 모티베이션 & 인트로

본 과제는 18개 기지국이 측정한 RTT 거리값으로부터 사용자의 2D 위치를 추정하는 실내 측위 문제다. 환경은 InF-DH FR1(Indoor Factory Dense, sub-6 GHz)로 NLOS가 흔하게 발생한다.

문제의 핵심은 측정값 d_hat이 부정확하다는 점이다. 700명 학습 데이터를 분석해 측정 오차 d_hat − d_true의 분포를 보면 강한 비대칭이 나타났다.

| 통계량 | 값 |
| --- | --- |
| 평균 오차 | +15.93 m |
| 중앙값 | +10.15 m |
| 최솟값 | −2.06 m |
| 최댓값 | +252.21 m |
| 음의 오차 비율 | 18.6 % |
| \|오차\| < 1 m (LOS 추정) | 29.9 % |
| 오차 > 5 m (NLOS 추정) | 59.0 % |

음의 오차는 절댓값이 모두 2 m 이내로 측정 잡음 수준이지만 양의 오차는 수십 m 이상이 흔하다. NLOS 환경의 물리적 특성을 그대로 반영하는 분포다. 신호가 장애물에 반사·회절되어 도달하는 경로는 직선보다 길 수밖에 없으므로 RTT 측정값은 실제 거리보다 짧아질 수 없다. 즉 d_hat은 거의 항상 실제 거리 이상이다.

이 사실이 가장 강력한 사전 정보다. 18개 기지국을 모두 사용한 단순 LS는 평균 오차 22.76 m로 측위가 거의 불가능한 수준이었는데, 모든 측정값에 동일한 가중치를 주는 방식이 NLOS 측정값에 끌려가기 때문이다.

기존의 NLOS 측위 접근들은 이 일방향 편향을 간접적으로 활용한다. Trimmed LS는 d_hat이 작은 측정값일수록 LOS일 가능성이 높다는 점을 이용해 일부를 버리고, 비대칭 손실 함수는 잔차의 부호에 따라 가중치를 다르게 부여한다. 두 방법 모두 NLOS 편향을 손실 함수 형태로 부드럽게 우회한다.

본 알고리즘은 한 단계 더 나아간다. NLOS 편향은 ‖p_bs_i − p‖ ≤ d_hat_i라는 부등식 형태로 정확히 표현되므로, 손실 함수로 부드럽게 페널티 주는 대신 부등식 제약 그 자체로 박아넣어 제약 최적화 문제로 풀이한다. NLOS와 LOS 식별이 별도의 단계 없이 KKT active set 분석으로 자동 수행된다. §4.2에서 이 active set이 실제로 LOS 기지국과 78.4 % 정확도로 일치함을 정량적으로 보인다.

---

## 2. 알고리즘 설명

본 알고리즘은 부등식 제약 최적화, Huber 손실, 반복적 가중치 갱신 세 가지를 결합한 구조다.

### 2.1 문제 정식화

i번째 기지국에 대한 잔차를 r_i = d_hat_i − ‖p_bs_i − x‖ 로 정의한다. 다음 가중 제약 최소화 문제를 푼다.

최소화: Σ_i w_i · ρ_H(r_i, k)

제약: ‖p_bs_i − x‖ ≤ d_hat_i + δ, 모든 i에 대해

ρ_H는 Huber 손실로, |r| ≤ k이면 (1/2)r², |r| > k이면 k·(|r| − k/2)이다. 잔차가 작을 때는 L2처럼 부드럽게 fit하고 잔차가 클 때는 L1처럼 선형 증가하여 극단적 NLOS 외란에 끌려가지 않는다. 일반 L2 손실보다 robust하다.

추정 위치 x는 모든 기지국에 대해 실제 거리가 측정값보다 작거나 같다는 부등식을 만족해야 한다. NLOS 측정값(d_hat이 부풀려진)에 대해서는 부등식이 여유 있게 만족되므로 추정에 거의 영향을 주지 않는다. LOS 측정값에 대해서는 부등식이 등호 근처에서 성립하면서 추정 위치를 강하게 제약한다.

### 2.2 Active Set이 LOS 식별기로 작동

KKT 조건에 따르면 최적해에서 부등식 제약은 두 그룹으로 나뉜다. 등호로 성립하는 active 제약과 strict 부등식으로 성립하는 inactive 제약이다. 본 문제에서는 active 제약이 LOS에 가까운 기지국, inactive 제약이 NLOS로 부풀려진 기지국에 대응한다. 기존 알고리즘들이 별도 분류 단계를 두는 반면 본 알고리즘은 최적화 자체가 LOS 식별을 동시에 수행한다.

### 2.3 반복적 가중치 갱신

§2.1의 풀이는 한 번으로 끝나지 않고 반복 안에서 작동한다. 매 반복 t에서 다음을 수행한다.

1단계: 현재 가중치 w^(t)로 §2.1을 풀어 위치 x^(t)를 얻는다.

2단계: 잔차 r_i^(t) = d_hat_i − ‖p_bs_i − x^(t)‖를 보고 가중치를 갱신한다.

w_i^(t+1) = exp( − max(r_i^(t), 0) / λ )

큰 양의 잔차를 가진 기지국은 NLOS 측정값으로 부풀려진 것이므로 다음 반복에서 가중치가 작아진다. 음수 잔차는 NLOS 신호가 아니라 LOS 잡음이므로 max(r, 0)으로 잘라 페널티를 주지 않는다.

가중치 초기값은 균등(모두 1)이고 반복은 5회다.

이 반복이 §2.2의 active set 정보를 부드럽게 활용한다. 단일 풀이에서 active set이 LOS BS를 식별하는 precision은 78.4 %인데 recall은 49.1 %에 그친다(§4.2). 즉 LOS BS 절반이 잡음 때문에 부등식 등호에서 벗어나 inactive로 분류된다. 가중치 기반 반복은 이 binary 구분을 continuous weight로 일반화하여 잔차가 0에 가까운 BS는 가중치가 1에 가깝게 유지되도록 한다.

### 2.4 외삽 사용자 적응형 처리

사용자 일부는 기지국 격자 [−50, 50] × [−20, 20] 외부에 위치한다. 외삽 영역 사용자는 가장 가까운 LOS 기지국까지의 거리가 멀어 RTT 측정에 더 큰 잡음이 섞인다. 따라서 LOS 측정값에 대한 부등식 tolerance를 키워야 SLSQP가 feasible 해를 찾을 수 있다.

본 알고리즘은 첫 풀이의 추정 x^(1)가 격자 외부이면 외삽 사용자로 판정하고, 이후 반복에서 δ를 기본값 δ_in = 0.7 m 대신 δ_out = 1.0 m로 키운다.

### 2.5 SLSQP 풀이와 안전장치

§2.1의 문제는 비선형 목적함수와 비선형 부등식 제약을 가진 일반적인 비선형 제약 최적화이며 SLSQP로 풀이한다. Huber 손실과 부등식의 그래디언트는 모두 해석적으로 계산하여 SLSQP에 명시적으로 전달한다. 첫 반복의 초기점은 1/d_hat²을 가중치로 한 기지국 좌표의 가중평균이고 이후 반복은 직전 결과를 초기점으로 사용한다. 추정 위치는 x ∈ [−80, 80] m, y ∈ [−50, 50] m 범위로 bound를 걸어 SLSQP가 비현실적인 영역으로 가지 않도록 한다.

### 2.6 하이퍼파라미터

본 알고리즘은 5개의 하이퍼파라미터를 가진다. 각각의 값은 임의로 정한 게 아니라 데이터 분석과 train/val split 검증으로 결정하였다.

δ_in과 δ_out은 LOS 측정값의 잡음 수준을 허용하는 tolerance다. 700명 데이터에서 음의 오차의 절댓값 분포를 BS별로 보면 95 백분위수가 모든 BS에서 1.06 ~ 1.24 m로 거의 균일하다. LOS 잡음이 BS 고유 특성이 아니라 RTT 측정 자체의 공통 특성이라는 의미이므로 BS별로 다른 δ_i를 둘 필요가 없다.

λ, k, n_outer는 train 500/val 200 split으로 스윕하여 결정하였다. 검증된 plateau와 채택값은 다음과 같다.

| 파라미터 | 검증된 plateau | 채택값 |
| --- | --- | --- |
| δ_in | [0.5, 0.8] m | 0.7 m |
| δ_out | [1.0, 1.5] m | 1.0 m |
| λ | [1.5, 2.0] m | 1.5 m |
| Huber k | [1.5, 2.5] m | 2.0 m |
| n_outer | [4, 6] | 5 |

모든 파라미터가 plateau 중앙에 위치하여 약간의 변동에도 성능이 안정적이다.

---

## 3. Agent AI 활용 방안

본 과제 진행 과정에서 대화형 AI인 Claude를 보조 도구로 활용하였다.

### 3.1 활용 방식

데이터 분석 단계에서 측정 오차의 통계와 분포를 살펴보는 작업에 AI를 활용하였다. 본인이 한국어로 "측정 오차의 부호 분포와 NLOS 추정 비율을 보고 싶다" 같은 분석 의도를 제시하면 AI가 그에 맞는 numpy·scipy 코드를 작성하고 결과 통계를 정리하였다.

알고리즘 설계 단계에서는 데이터 분석 결과를 함께 보면서 어떤 사전 지식이 활용 가능한지 논의하였다. NLOS 일방향 편향을 활용하는 단순한 방법들(Trimmed LS, 비대칭 손실 함수)을 먼저 구현해본 뒤, 부등식 자체를 직접 박을 수는 없을까라는 본인의 아이디어로부터 제약 최적화 접근을 결정하였다. AI는 이 아이디어를 SLSQP를 사용한 구체적인 정식화로 옮기는 데 도움을 주었다.

구현 단계에서 scipy.optimize.minimize에 부등식 제약과 그래디언트를 전달하는 코드 구조 작성을 AI가 도왔다. 초기 정식화에서 slack 변수에 강한 페널티를 주는 형태로 짰는데 해가 엉뚱한 곳으로 가는 문제가 있었다. 본인이 정식화를 다시 검토해 ε 변수를 제거하고 부등식을 직접 박는 형태로 수정하였다.

검증 단계에서 active set이 LOS BS를 식별하는지 정량적으로 측정하는 코드를 AI가 작성하였다. recall이 49 %에 그친다는 결과를 본 뒤 active 정보를 hard binary가 아닌 continuous weight로 활용하면 어떨까라는 아이디어를 본인이 떠올렸고, 이것이 §2.3의 반복적 가중치 갱신 구조로 이어졌다. L2 손실 대신 Huber 손실을 시도하자는 결정도 본인이 내렸다. 모든 하이퍼파라미터의 train/val split 검증 코드는 AI가 작성하였으나 결과 해석과 채택값 확정은 본인이 결정하였다.

§4.6의 hold-out 검증도 본인이 직접 설계하였다. 모든 튜닝이 데이터 의존적일 가능성을 점검하기 위해 새 시드로 train/hold-out을 분할하고 train만으로 하이퍼파라미터를 다시 결정한 뒤 hold-out에서 한 번만 평가하는 방식을 본인이 제안하였다.

### 3.2 역할 구분

본인은 알고리즘의 방향성과 핵심 아이디어, 결과 해석을 담당하였다. NLOS 일방향 편향을 알고리즘 설계의 출발점으로 삼는 결정, 부드러운 손실 함수 대신 hard constraint를 쓰기로 한 결정, active set 검증 결과를 보고 반복적 가중치 갱신을 추가한 결정, L2를 Huber로 교체한 결정, 외삽 사용자에 적응형 δ를 도입한 결정 모두 본인이 내렸다.

AI는 본인이 결정한 아이디어를 scipy 인터페이스에 맞는 코드로 옮기는 작업, 그래디언트의 해석적 계산을 도와주는 작업, 결과 통계를 정리하는 작업을 맡았다. 알고리즘의 핵심 구조(부등식 제약, active set 해석, 반복적 가중치 갱신, Huber 손실)는 본인이 데이터 분석과 검증 실험에서 도출한 통찰을 토대로 결정한 것이다.

---

## 4. 결과 도출 & 디스커션

### 4.1 측위 성능

700명 학습용 데이터 전체에 대한 본 알고리즘과 베이스라인의 측위 오차는 다음과 같다.

| 알고리즘 | 평균 (m) | 중앙값 (m) | 90 백분위수 (m) | 최대 (m) |
| --- | --- | --- | --- | --- |
| LS-18 (모든 기지국 사용) | 22.76 | 21.64 | 32.55 | — |
| Trimmed LS k=3 | 9.91 | 6.29 | 25.66 | — |
| Asymmetric Loss α/β=200 | 1.76 | 0.90 | 3.33 | — |
| Constrained Optimization (1회 풀이) | 1.79 | 0.89 | 3.32 | — |
| Iterative Reweighted Constrained (L2) | 1.40 | 0.74 | 1.83 | 35.2 |
| + Huber + Adaptive δ (제안) | 1.16 | 0.67 | 1.44 | 26.3 |

LS-18 대비 평균을 94.9 %, 중앙값을 96.9 % 줄였다. Iterative L2 버전에 Huber 손실과 외삽 적응형 δ를 추가하여 추가로 평균 17.1 %, 90 백분위수 21.3 %, 최대 오차 25.3 %의 개선을 얻었다. 최대 오차의 큰 감소는 Huber 손실이 NLOS 외란에 강건하다는 특성을 잘 보여준다.

700명 전체 실행 시간은 약 4.3초, 사용자당 평균 6 ms로 채점 환경의 10분 제한에 비해 매우 빠르다.

### 4.2 Active Set 검증

본 알고리즘의 핵심 주장은 active 제약이 LOS BS를 식별한다는 것이다. 이를 정량적으로 검증하기 위해 단일 풀이 버전(반복 없음) 결과를 분석하였다. 700명 데이터에 대해 알고리즘을 풀고 최적해에서 slack이 0.5 m 미만인 제약을 active로 분류하였다. 동시에 정답 위치로 |오차| < 1 m인 측정값을 실제 LOS로 분류하였다.

| 지표 | 값 |
| --- | --- |
| Precision (active 중 LOS 비율) | 78.4 % |
| Recall (LOS 중 active 비율) | 49.1 % |
| 사용자당 평균 active 제약 수 | 3.4 |
| 사용자당 평균 LOS BS 수 | 5.4 |

Precision 78.4 %는 active로 식별된 제약 중 약 5분의 4가 실제 LOS BS임을 의미한다. active set이 LOS 분류기로 잘 작동한다는 정량적 증거다. Recall이 49.1 %인 이유는 일부 LOS 측정값이 잡음으로 부등식 등호에서 벗어나 inactive로 분류되기 때문이다. 이 비대칭이 §2.3의 반복적 가중치 갱신 동기다.

### 4.3 본인 사고와 구현의 적합성

본 알고리즘의 핵심은 세 가지다. NLOS 일방향 편향을 부등식 제약으로 직접 인코딩하기, active set 정보를 잔차 기반 continuous weight로 부드럽게 활용하기, Huber 손실로 외란에 강건하게 만들기. 세 가지 모두 데이터 분석과 검증 실험에서 발견한 사실에서 도출되었다.

첫째 아이디어의 근거는 측정값의 81.4 %가 d_true 이상이라는 통계와 NLOS의 물리적 특성이다. 둘째 아이디어의 근거는 §4.2의 active set 검증에서 발견한 precision 78 % / recall 49 %의 비대칭이다. recall이 낮다는 사실 자체가 활용할 정보가 남아 있다는 신호였다. 셋째 아이디어의 근거는 측정 오차의 매우 긴 양수 꼬리다. 최댓값이 +252 m에 달하는 극단적인 외란이 존재하므로 L2 손실보다 L1처럼 작동하는 Huber 손실이 적합하다.

모든 하이퍼파라미터가 데이터 기반으로 결정되었다. δ는 BS별 음의 오차 분포 분석에서 도출되었고 λ, k, n_outer는 train/val split으로 검증되었다. 700명 전체에 대한 직접 튜닝으로 결정된 값은 없다.

구현 측면에서 SLSQP는 비선형 부등식 제약 최적화의 표준 방법이며 scipy에 안정적으로 구현되어 있다. Huber 손실과 부등식 제약의 그래디언트를 모두 해석적으로 계산해 전달하여 수치 미분의 부정확성을 피하고 수렴을 안정화하였다.

### 4.4 Baseline 비교의 공정성

본 알고리즘은 머신러닝을 사용하지 않는 비ML 방법이다. 비교 대상인 LS-18, Trimmed LS, Asymmetric Loss, 단일 풀이 Constrained Optimization 모두 같은 계열의 비ML 방법이며 동일한 데이터에 동일한 평가 지표로 측정되었다. 입력 데이터, 초기값 생성 방식, 좌표 클립 같은 공통 안전장치까지 모두 같다. 차이는 사전 지식을 활용하는 방식뿐이다.

복잡한 신경망이나 학습 기반 방법과 비교하지 않은 이유는 두 가지다. 학습 기반 방법은 700명에 over-fit할 위험이 있어 hidden 300명에 대한 일반화 성능이 떨어질 수 있고, 본 비교의 목적이 NLOS 일방향 편향이라는 사전 지식이 비ML 추정기 안에서 어떻게 표현되어야 효과적인지 살피는 것이었기 때문이다.

### 4.5 장점과 한계

장점은 사전 지식이 부등식 제약으로 정확히 인코딩되어 NLOS와 LOS 식별이 active set 분석으로 자동 수행된다는 것, Huber 손실로 외란에 강건하다는 것, 외삽 사용자에 자동으로 적응한다는 것이다. 또한 학습 단계가 없고 5개 하이퍼파라미터가 모두 데이터 기반으로 결정되어 over-fit 위험이 작다. 실행도 빠르다(사용자당 6 ms).

한계는 두 가지다. 어려운 사용자에서 잘못된 local minimum에 빠지는 경우가 남아 있어 최대 26 m 오차가 잔존한다. 또한 SLSQP가 모든 제약을 동시에 만족하는 해를 찾지 못하는 feasibility 위험이 일부 사용자에서 발생할 수 있다.

### 4.6 평가 방식의 공정성과 일반화 검증

본 보고서에서 제시한 모든 성능 수치는 학습용 700명 전체에 대한 측위 오차다. 채점이 hidden 300명으로 진행되므로 over-fit 가능성을 두 단계로 검증하였다.

첫째 §2.6의 train 500/val 200 split에서 모든 하이퍼파라미터가 train과 val 모두에서 안정적인 plateau를 이룬다는 것을 확인하였다.

둘째 새로운 시드로 train 560/hold-out 140을 분할하여 train만으로 하이퍼파라미터를 다시 결정한 뒤 hold-out에서 한 번만 평가하였다.

| Dataset | N | 평균 (m) | 중앙값 (m) | 90 백분위수 (m) |
| --- | --- | --- | --- | --- |
| Train | 560 | 1.16 | 0.64 | 1.48 |
| Hold-out | 140 | 1.09 | 0.66 | 1.30 |

Hold-out과 train의 평균 차이는 −6 %로 hold-out이 오히려 약간 더 좋고 중앙값 차이는 +4 %로 사실상 동일하다. 본 알고리즘이 학습 데이터에 over-fit되지 않았으며 보지 않은 사용자에 대해서도 일관된 성능을 낸다는 의미다. hidden 300명에 대해서도 유사한 성능을 기대할 수 있다.

평가 지표는 추정 위치와 정답 위치의 유클리드 거리이며 평균, 중앙값, 90 백분위수를 함께 보고하였다. 평균만 보면 일부 어려운 사용자가 결과를 왜곡할 수 있고 중앙값만 보면 꼬리 영역이 가려진다. 세 지표를 함께 봐야 알고리즘이 일반적 사용자와 어려운 사용자 모두에서 어떻게 작동하는지 파악할 수 있다.

### 4.7 Future work

남은 한계 중 가장 큰 것은 어려운 사용자에서 잔존하는 26 m 수준의 오차다. 분석해 보면 알고리즘이 잘못된 local minimum에 빠져 잔차는 작지만 실제 위치가 아닌 곳에 도달하는 경우가 대부분이다. 잔차로는 어떤 추정이 잘못되었는지 판별하기 어렵다. 이 한계를 극복하려면 잔차 이외의 다른 정보, 예를 들어 사용자 위치에 대한 사전 분포나 인접 사용자와의 공간적 상관관계를 활용해야 할 것이다. 본 데이터셋은 사용자 간 독립성을 가정하므로 이 방향은 본 과제의 범위 밖이다.

다른 방향으로 본 알고리즘은 모든 부등식을 hard constraint로 다루는데 일부 사용자에서 feasibility 문제가 생길 수 있다. slack 변수와 적절한 페널티 항을 추가해 soft constraint로 일반화하면 이 한계를 완화할 수 있고, 이 경우 본 알고리즘과 비대칭 손실 함수가 같은 일반화의 양 극단으로 통합되는 관점이 가능해진다.

---

## 5. Reference

본 보고서의 알고리즘은 특정 NLOS 측위 논문을 직접 차용하지 않고 데이터 분석에서 도출된 관찰만으로 설계하였다. 다만 알고리즘 구성에 사용된 표준적인 수치 최적화 기법과 robust statistics 도구들은 다음 자료를 참고하였다.

[1] Kraft, D. (1988). *A software package for sequential quadratic programming*. DFVLR-FB 88-28, Deutsche Forschungs- und Versuchsanstalt für Luft- und Raumfahrt, Köln, Germany. — SLSQP 알고리즘의 원 논문. 본 과제에서 비선형 부등식 제약 최적화 풀이에 사용.

[2] Huber, P. J. (1964). *Robust estimation of a location parameter*. The Annals of Mathematical Statistics, 35(1), 73–101. — Huber 손실 함수의 정의와 robust 추정 이론.

[3] Holland, P. W., & Welsch, R. E. (1977). *Robust regression using iteratively reweighted least-squares*. Communications in Statistics — Theory and Methods, 6(9), 813–827. — IRLS(Iteratively Reweighted Least Squares) 기법.

[4] Boyd, S., & Vandenberghe, L. (2004). *Convex Optimization*. Cambridge University Press. — KKT 조건과 active set 분석의 표준 교과서.

[5] Virtanen, P. et al. (2020). *SciPy 1.0: Fundamental algorithms for scientific computing in Python*. Nature Methods, 17, 261–272. — `scipy.optimize.minimize`의 SLSQP 구현 사용.

NLOS 측위에 부등식 제약을 hard constraint로 직접 인코딩하고 KKT active set을 LOS 식별기로 해석하며 잔차 기반 continuous weight 갱신과 Huber 손실을 결합한 본 알고리즘의 구조는 위 표준 도구들을 본 과제의 데이터 특성에 맞춰 본인이 설계한 것이다.
